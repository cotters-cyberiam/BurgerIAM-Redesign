apiVersion: apps/v1
kind: Deployment
metadata:
  name: burgeriam-notification-service
  namespace: burgeriam
spec:
  replicas: 1
  selector:
    matchLabels:
      app: burgeriam-notification-service
  template:
    metadata:
      labels:
        app: burgeriam-notification-service
    spec:
      containers:
        - name: notification-service
          image: cotters07/burgeriam-notification-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5018
              name: grpc
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:5018"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__DefaultConnection
              value: "Data Source=/app/data/notification.db"
            - name: EventBus__ConnectionString
              value: "amqp://guest:guest@burgeriam-rabbitmq:5672"
            - name: EventBus__ExchangeName
              value: "burgeriam.exchange"
          volumeMounts:
            - name: data
              mountPath: /app/data
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 5018
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 5018
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: notification-service-data
---
apiVersion: v1
kind: Service
metadata:
  name: burgeriam-notification-service
  namespace: burgeriam
spec:
  selector:
    app: burgeriam-notification-service
  ports:
    - name: grpc
      port: 5018
      targetPort: 5018
  type: ClusterIP
