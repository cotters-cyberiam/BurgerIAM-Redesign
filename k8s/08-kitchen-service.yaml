apiVersion: apps/v1
kind: Deployment
metadata:
  name: burgeriam-kitchen-service
  namespace: burgeriam
spec:
  replicas: 1
  selector:
    matchLabels:
      app: burgeriam-kitchen-service
  template:
    metadata:
      labels:
        app: burgeriam-kitchen-service
    spec:
      containers:
        - name: kitchen-service
          image: cotters07/burgeriam-kitchen-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5085
              name: grpc
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:5085"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__DefaultConnection
              value: "Data Source=/app/data/kitchen.db"
            - name: EventBus__ConnectionString
              value: "amqp://guest:guest@burgeriam-rabbitmq:5672"
            - name: EventBus__ExchangeName
              value: "burgeriam.exchange"
          volumeMounts:
            - name: data
              mountPath: /app/data
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 5085
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 5085
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: kitchen-service-data
---
apiVersion: v1
kind: Service
metadata:
  name: burgeriam-kitchen-service
  namespace: burgeriam
spec:
  selector:
    app: burgeriam-kitchen-service
  ports:
    - name: grpc
      port: 5085
      targetPort: 5085
  type: ClusterIP
